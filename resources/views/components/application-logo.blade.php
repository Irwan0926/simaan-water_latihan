@props(['class' => ''])

<span class="inline-flex items-center gap-2 {{ $class }}">
    <span class="font-semibold tracking-tight text-current {{ $attributes->get('textClass', '') }}">Simaan Water</span>
</span>